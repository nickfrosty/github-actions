# to set an ENV value, there must be a FROM already declared
# FROM scratch

ARG SOLANA_VERSION

# FROM ubuntu:20.04
FROM solanafoundation/solana-verifiable-build:${SOLANA_VERSION}

ENV SOLANA_VERSION=${SOLANA_VERSION}
